% =============================================================================
%   V. RESULTS
% =============================================================================
\section{Results}
\label{sec:results}

\subsection{RQ1: What the ESG Score Measures}
\label{sec:res_construct}

\textbf{Sources.} Table~\ref{tab:provenance} shows how little of the ESG
schema is populated by ESG data. The E pillar has four informative
indicators:
\begin{itemize}
\item three within-sector financial percentiles: operating margin, standing
  in for emissions; revenue-to-market-value, standing in for renewable share;
  and gross margin, standing in for waste;
\item one binary flag for public climate commitments.
\end{itemize}
The S pillar has seven indicators, six of them financial. Examples are
revenue per employee (which enters twice, once as a ratio and once as a
proxy for employee satisfaction), dividend-plus-FCF yield for community
investment, and payout-plus-current-ratio for supply-chain audits. The G
pillar differs. \ISSShareG\% of its cells are ISS governance risk scores,
and the rest are ownership, analyst-coverage and SEC items or imputations.

\textbf{Redundancy.} Regressing each pillar on the other seven factor scores
gives $R^2=\RSqE$ (E), \RSqS (S) and \RSqG (G); for the composite it is
\RSqESG. The ESG composite has a Spearman correlation of \RhoESGFin with the
financial score. The pillars are nearly unrelated to each other: E--S
$\rho=\RhoES$, E--G $\rho=\RhoEG$, S--G $\rho=\RhoSG$. Hence ``ESG'' here is
two different objects:
\begin{itemize}
\item E and S: financial quality measured within sector;
\item G: largely independent third-party governance information.
\end{itemize}
Conventional diagnostics do not reveal this. The maximum VIF among factor
scores is only \VIFMax (\VIFMaxFactor), and the first three principal
components explain \PCAFirstThree\% of factor variance, which looks like a
well-conditioned, multidimensional factor set. The redundancy sits
\emph{inside} the ESG block, where the financial proxies duplicate
information already present in the financial factors.

\textbf{Profiles.} The three investor profiles are highly concordant. The
balanced composite has Spearman correlations of \RhoProfEB with the
ESG-first and \RhoProfBF with the financial-first composite. The top-\TopN
lists share \OverlapEB and \OverlapBF names, respectively. Changing the ESG
weight from \WFinFirstESG\% to \WESGFirstESG\% therefore changes the ranking
far less than the labels suggest.

\subsection{RQ2: In-Sample Validation Overstates Quality}
\label{sec:res_insample}

Table~\ref{tab:insample} reports the pipeline's former validation evidence
next to the OOS outcome. The balanced composite's in-sample correlation with
the trailing six-month return is \InSampleICBalSixM. This is not evidence of
prediction: the ``returns'' end on the snapshot date, and the composite
contains risk-adjusted and stability scores built from the same price
history. The correlation with the contemporaneous quality proxy is
\InSampleICBalQuality. It is inflated further by shared inputs, since the
proxy is built from ROA, ROE, margins and growth, which also feed the
financial, growth and operational factors. Out of sample, all three profiles
have negative point ICs, and every confidence interval contains zero.

Table~\ref{tab:prepost} isolates three further artefacts.
\begin{enumerate}
\item The isotonic monotonicity correction was fitted to the quality proxy.
  With it, \MonoPre of \MonoTotal factors were ``monotonic'' in that proxy;
  without it, \MonoPost are.
\item The former ``Bayesian bootstrap'' multiplied each firm's score by
  i.i.d.\ Exponential(1) noise. The resulting rank intervals had a median
  width of \PreCIWidthMedian positions out of \PreNFirmsBoot. The paper
  nonetheless reported perfect rank stability (Kendall $\tau=1$) from a
  separate calculation.
\item The corrected composite shares only \OverlapPrePost of its top-\TopN
  names with the pre-audit version, although the Spearman correlation of the
  two is \RhoPrePostBal. The direction errors changed the E pillar most
  (pre--post $\rho=\RhoPrePostE$).
\end{enumerate}
None of these artefacts changed the OOS conclusion: the pre-audit composite
also has no predictive power (IC $=\ICPreAuditFull$). They changed only the
\emph{evidence} that was offered for the composite.

\begin{table}[t]
\centering
\caption{In-sample ``validation'' versus out-of-sample outcome. In-sample
columns are rank correlations of the composite with trailing six-month returns
and with a contemporaneous quality proxy on the snapshot cross-section.}
\label{tab:insample}
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_insample_vs_oos}}
\end{table}

\begin{table}[t]
\centering
\caption{Diagnostics before and after the audit corrections of
Section~\ref{sec:audit}. Pre-audit rank intervals come from the former
``Bayesian bootstrap''; post-audit intervals from Section~\ref{sec:rank_design}.}
\label{tab:prepost}
\scriptsize
\input{generated/tab_pre_post}
\end{table}

\subsection{RQ3: Out-of-Sample Evidence}
\label{sec:res_oos}

Over the window, the S\&P~500 returned \IdxRetUS\% and the NIFTY~50
\IdxRetIndia\%. The equal-weighted mid-cap universes returned \UnivRetUS\%
(US) and \UnivRetIndia\% (India). Table~\ref{tab:primary} reports the three
primary hypotheses.

\begin{table}[t]
\centering
\caption{Primary hypotheses (one-sided tests, Holm-adjusted across H1--H3;
$N=\NOOS$ firms with a full-window return).}
\label{tab:primary}
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_oos_primary}}
\end{table}

\textbf{H1 and H3 (returns): not supported.} The balanced composite's IC with
the country-neutral full-window return is \HOneEst (95\% CI
$[\HOneLo,\HOneHi]$). Its top-\TopN portfolio beat the universe by
\HThreeEst percentage points (pp), with CI $[\HThreeLo,\HThreeHi]$. Both
estimates are robust to the \NExited M\&A exits: under the survivorship
bounds, the IC lies in $[\BoundICLow,\BoundICHigh]$ and the excess in
$[\BoundExLow,\BoundExHigh]$~pp. The top-ranked firm (CTRA) was among the
exits.

\textbf{H2 (risk): supported.} Controlling for trailing volatility, higher
ESG predicts lower realised volatility: partial $\rho=\HTwoEst$, 95\% CI
$[\HTwoLo,\HTwoHi]$, Holm $p=\HTwoHolm$. The effect is economically modest.
Trailing volatility alone has a rank correlation of \RhoVolPersist with
realised volatility, so most of the raw ESG--volatility association
($\rho=\RhoESGVolRaw$) is past volatility carried forward.

\textbf{Secondary evidence.} Figure~\ref{fig:oos_ic} and
Table~\ref{tab:oos_ic} give ICs for all factors and composites. No composite
has a positive IC at any horizon. The only Holm-significant full-window IC is
negative, for the S pillar (\ICSFull). At one month, the financial and market
scores have significantly \emph{negative} ICs, consistent with short-term
reversal. Table~\ref{tab:oos_risk} decomposes the risk effect:
\begin{itemize}
\item In rank regressions with trailing volatility, trailing beta, size, and
  sector and country fixed effects, the composite (\RegVolESG, $p=\RegVolPESG$)
  and the S pillar (\RegVolS, $p=\RegVolPS$) predict lower realised
  volatility.
\item The G pillar has a pooled partial correlation of \PartBetaG with
  realised \emph{beta} but none with volatility (\RegVolG, $p=\RegVolPG$).
  The beta association is a pooling effect: it disappears once sector and
  country fixed effects are added (Table~\ref{tab:ext_controls}).
\item The E pillar predicts neither.
\item The part of ESG that is orthogonal to the other factors predicts
  neither at the 5\% level.
\end{itemize}
The forward risk signal is therefore carried mostly by the financial-proxy S
pillar. Section~\ref{sec:res_mechanism} asks what that signal is.

\textbf{The cost of the tilt.} Table~\ref{tab:oos_port} shows what the risk
tilt cost in a rising market. The top-\TopN ESG-only portfolio's excess return
over the universe was \ExESGFull~pp (CI $[\ExESGFullLo,\ExESGFullHi]$). Adjusting for
trailing beta leaves \ExESGBA~pp. Sector-and-country neutralisation reduces
the gap to \ExESGSN~pp (CI $[\ExESGSNLo,\ExESGSNHi]$): roughly half of the
shortfall is a sector bet. The portfolio overweights utilities and energy and
underweights technology, which was the best-performing sector.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{fig_oos_ic}
\caption{Out-of-sample rank ICs over (\SnapshotDate,\,\OOSEndDate] with 95\%
bootstrap intervals. Blue: composites; green: ESG measures; grey: financial
factors (*market score carries zero weight). Two-sided 80\%-power detection
threshold: $|\rho|\ge\MDEOOS$.}
\label{fig:oos_ic}
\end{figure}

\begin{table}[t]
\centering
\caption{Out-of-sample rank ICs with country-neutral returns. Bold: Holm
$p<0.05$ within horizon.}
\label{tab:oos_ic}
\scriptsize
\setlength{\tabcolsep}{3pt}
\input{generated/tab_oos_ic}
\end{table}

\begin{table}[t]
\centering
\caption{Top-\TopN portfolio excess return over the equal-weighted universe
(pp, country-neutral). $\beta$-adj.: market-model adjustment with trailing
beta; Sect.-neut.: sector-and-country-neutral full-window returns.}
\label{tab:oos_port}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_oos_portfolio}}
\end{table}

\begin{table}[t]
\centering
\caption{ESG and realised risk. Partial $\rho$ controls for the trailing value
of the same risk measure; bold: Holm $p<0.05$ across the 15 secondary tests.
Regression: rank of realised volatility on ESG rank with trailing volatility,
beta, size, and sector and country effects (HC3).}
\label{tab:oos_risk}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_oos_risk}}
\end{table}

\subsection{RQ4: Where the Risk Signal Comes From}
\label{sec:res_mechanism}

H2 is the only positive result, so we test it hardest.
Tables~\ref{tab:ext_controls} and~\ref{tab:ext_robust} report the evidence,
which gives a three-part answer.

\textbf{It survives known risk factors.} With full controls (trailing
volatility and beta, size, quality, leverage, liquidity, momentum, value, and
sector and country effects), the ESG coefficient is \HFourEst (95\% CI
$[\HFourLo,\HFourHi]$, one-sided $p=\HFourP$; H4). Two further checks agree.
\begin{itemize}
\item \emph{Double sort.} Within trailing-volatility terciles, high-ESG
  firms had \DSAvg~pp lower realised volatility than low-ESG firms (CI
  $[\DSLo,\DSHi]$). The gap grows from \DSLowTerc~pp in the calmest tercile
  to \DSHighTerc~pp in the most volatile.
\item \emph{Robustness.} The effect holds without the \NPinned takeover-pinned
  firms, with winsorised volatility, without utilities and real estate, and
  has the same sign in both countries (US \HTwoUS, India \HTwoIndia).
\end{itemize}
The ESG--beta association does not survive: it falls from \BetaTrailESG
($p=\BetaTrailESGP$) with the trailing control to \BetaFullESG
($p=\BetaFullESGP$) with full controls. No ESG measure predicts drawdowns.

\textbf{It is not in the measured ESG data.} The signal sits in the S pillar
(\HFourS, $p=\HFourSP$), whose inputs are almost all financial proxies. The
G pillar (\HFourG) carries none of it. Splitting each firm's cells by source
makes this sharper:
\begin{itemize}
\item \emph{Measured cells} (ISS, SEC, registry) have partial
  $\rho=\PartMeas$ with realised volatility (CI $[\PartMeasLo,\PartMeasHi]$),
  and are nearly independent of the financial factors ($R^2=\RSqMeas$).
\item \emph{Proxy cells} are largely financial ($R^2=\RSqProxy$) and have
  partial $\rho=\PartProxy$ (CI $[\PartProxyLo,\PartProxyHi]$).
\item \emph{Imputed cells} have a strong raw association (\PartImp), which
  is a sector effect: it vanishes once sector fixed effects are included
  (Table~\ref{tab:ext_controls}).
\end{itemize}
The two non-imputed sub-scores are almost unrelated ($\rho=\RhoMeasProxy$).
Adding measured-cell ESG to the out-of-fold volatility forecast changes $R^2$
by \CVDeltaMeas.

\textbf{It is small, and partly stale volatility.} Adding the composite to a
cross-validated volatility forecast raises out-of-fold $R^2$ from \CVBaseRSq
to \CVESGRSq, an improvement in \CVShareImproved\% of repeats but only
\CVDeltaESG in size. Recent volatility erodes it further. Given pre-snapshot
volatility, ESG's partial correlation with second-half volatility is
\RecTrail. Given first-half realised volatility it is \RecFirst (CI
$[\RecFirstLo,\RecFirstHi]$, $p=\RecFirstP$), and given both it is \RecBoth.
The monthly partial correlations are negative in \MonNegPartial of
\NMonths months, but each is small (\MonPartMin to \MonPartMax). ESG
therefore behaves like a slow-moving summary of a firm's risk profile. It
adds a little beyond long-horizon trailing volatility and little beyond
recent volatility.

\begin{table}[t]
\centering
\caption{Rank-regression coefficients of each ESG measure for realised risk.
``Trailing'': control for the pre-snapshot value of the same risk measure;
``full'': all controls of Section~\ref{sec:ext_design}, with sector and
country fixed effects (HC3). Drawdown is signed, so a positive value means a
shallower drawdown. Bold: $p<0.05$ (two-sided, unadjusted). $N=\NOOS$.}
\label{tab:ext_controls}
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_ext_controls}}
\end{table}

\begin{table}[t]
\centering
\caption{Robustness of the ESG--volatility result. Upper panel: H2 and H4 on
subsamples. Lower panel: partial Spearman correlation of ESG with realised
volatility after \SplitDate, given earlier volatility.}
\label{tab:ext_robust}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_ext_robust}}
\end{table}

\subsection{Robustness of the Return Null}
\label{sec:res_null}

The failure of H1 is not a consequence of the chosen weights
(Figure~\ref{fig:ext_weights}).
\begin{itemize}
\item \emph{Random weights.} Across \WDraws weight vectors drawn uniformly
  from the simplex, the OOS IC has median \WICMedian (2.5--97.5\% range
  $[\WICLo,\WICHi]$) and is positive in only \WSharePos\% of draws. Around
  the deployed weights ($\kappa=\BootKappa$), it is positive in
  \WKapSharePos\% of draws.
\item \emph{ESG weight.} More ESG weight lowers the IC (rank correlation
  \WCorrESGIC across draws). The IC moves from \SweepICZero with no ESG to
  \SweepICOne with ESG only.
\item \emph{Low volatility without ESG.} The top-\TopN portfolio has lower
  realised volatility than the universe in \WShareLowVol\% of draws, whatever
  the ESG weight (rank correlation \WCorrESGVol). With no ESG weight, its
  volatility is already \SweepVolZero~pp below the universe. The
  composite's low-risk profile comes from its risk-adjusted and stability
  factors, not from ESG.
\item \emph{Over time.} Monthly ICs of the balanced composite are negative
  in \MonNegICBal of \NMonths months (range \MonICMin to \MonICMax).
\item \emph{Costs.} Round-trip costs for the top-\TopN portfolio are
  \CostRT~pp, which leaves a net excess of \NetExBase~pp (\NetExStress~pp at
  three times the costs). The sign of H3 is thus unaffected, and it remains
  insignificant.
\end{itemize}

\begin{figure*}[t]
\centering
\includegraphics[width=0.9\textwidth]{fig_ext_weights}
\caption{Weight uncertainty and the ESG weight. (a) OOS IC of the composite
under \WDraws weight vectors drawn uniformly from the simplex; dashed line:
deployed weights. (b) OOS IC as the ESG weight rises from 0 to 1, with the
other weights in their deployed proportions (95\% bootstrap band). (c) The
top-\TopN portfolio's excess return and realised volatility relative to the
universe on the same sweep. Dotted line: deployed ESG weight.}
\label{fig:ext_weights}
\end{figure*}

\subsection{Rank Uncertainty}
\label{sec:res_rank}

Figure~\ref{fig:rank_ci} and Table~\ref{tab:top} report rank intervals for
the balanced profile. For the top-\TopN names, the median 95\% interval spans
\CIWidthTop ranks (maximum \CIWidthTopMax). Weight uncertainty dominates:
alone it gives \CIWidthTopWeights ranks, against \CIWidthTopSampling for
universe resampling. \NPTopHalf of the \TopN current constituents remain in
the top-\TopN in at least half of the draws, and the expected overlap between
the published and a perturbed portfolio is \ExpOverlap names. Rankings near
the top are therefore reasonably determined \emph{given} the construction;
the OOS results show that the construction itself does not predict returns.

\begin{figure}[t]
\centering
\includegraphics[width=0.9\columnwidth]{fig_rank_ci}
\caption{Balanced-profile ranks with 95\% intervals combining universe
resampling and Dirichlet ($\kappa=\BootKappa$) weight perturbation
($B=\BootN$). *NSE-listed.}
\label{fig:rank_ci}
\end{figure}

\begin{table}[t]
\centering
\caption{Top-\TopN balanced portfolio: 95\% rank interval, probability of
remaining in the top-\TopN, and realised full-window return ($^\dagger$NSE;
``exit'': acquired during the window).}
\label{tab:top}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\resizebox{\columnwidth}{!}{\input{generated/tab_top_ranks}}
\end{table}
