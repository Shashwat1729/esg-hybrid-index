"""
test_time_splits.py
Disabled unit tests: per user request the repository will use iterative script runs rather than pytest unit tests. The time-split evaluation module remains available in `scripts/07_time_split_evaluation.py` for ad-hoc checks.
"""
